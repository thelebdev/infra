# Applications

This directory contains:

- **`registry.yml`** — the canonical list of applications currently deployed (or planned) on this platform.
- **`<app-name>.md`** — one file per application describing its integration with the platform.

Application code and application-specific infrastructure live in their own repositories, not here. This directory documents the platform-level view of those applications.
